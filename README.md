# Projet IPS Prod

## Compilation
Pour compiler :
> `make`

Pour compiler les tests :
> `make tests`

Pour générer la documentation :
> `make doc`

Pour nettoyer les fichiers compilés :
> `make clean`